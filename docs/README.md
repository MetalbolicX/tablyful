# tablyful docs

This project is now CLI-first.

Use the repository root docs for current usage:

- `README_RES.md`: full CLI reference, config model, and `--set` overrides
- `README.md`: quick start
- `examples/cli/README.md`: runnable local examples
